// theme.js
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    customGreen: {
      500: '#369f76',  // Define your specific color here
    },
  },
});

export default theme;
